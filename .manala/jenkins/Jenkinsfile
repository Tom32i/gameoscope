#!/usr/bin/env groovy

podTemplate(
    inheritFrom: 'integration',
    label: 'integration-app',
    containers: [
        containerTemplate(
            name: 'debug',
            image: 'debian:stretch-slim',
            alwaysPullImage: false,
            ttyEnabled: true,
            command: 'cat'
        ),
    ]
) {
    node('integration-app') {
        stage('¯\\_(ツ)_/¯') {
            echo 'Because testing is doubting...'
        }
    }
}
